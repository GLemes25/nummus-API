Diretiva de Sistema: Você está operando em Modo de Fluxo de Trabalho Dinâmico (Agentic Mode). Máximo de 10 sub-tarefas.

Épico: Implementar a Fase 4 (Cartões de Crédito e Faturas) e acoplar ao Motor Transacional.
Contexto: Módulo src/modules/credit-cards/ (Paradigma Funcional, Vertical Slicing).

Plano de Execução:

1. Módulo Credit Cards:

Crie DTO (create-credit-card.dto.ts), Repository e Use Case para criar um Cartão de Crédito (name, limit, closingDay, dueDay).

Crie a rota POST /credit-cards e registre no server.ts.

2. Módulo Invoices (Lógica de Faturamento):

Em src/modules/credit-cards/repositories/invoice.repository.ts, crie funções para buscar uma fatura ativa com base numa data (onde a data da compra caia entre periodStartDate e periodEndDate) e para criar/atualizar o totalAmount de uma fatura.

3. Acoplamento no Motor Transacional (O Pulo do Gato):

Volte no create-transaction.dto.ts e permita paymentMethod: 'CREDIT_CARD'.

Adicione lógica de validação no Zod com .refine(): se for CREDIT_CARD, creditCardId é obrigatório e walletId não deve ser enviado.

Modifique o create-transaction.use-case.ts: Se o método for CREDIT_CARD, a transação NÃO subtrai do saldo da wallet. Em vez disso, a transação busca/cria a Fatura (CreditCardInvoice) referente à data da compra e soma o valor no invoice.totalAmount. Adicione a atualização da fatura ao prisma.$transaction.

Regras Absolutas:

Zero Classes.

Clean Code: Todo o código 100% em Inglês.

Rode npx tsc --noEmit internamente para garantir que a tipagem do Prisma aceita os relacionamentos novos.

Ao finalizar sem erros, gere o Git commit em Inglês ("feat(credit): implement credit card management and invoice routing for transactions"). Reporte de volta.
