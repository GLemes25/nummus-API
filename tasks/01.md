Não, para criar a API exatamente com esse modelo do Fastify que você enviou, **nós não vamos usar o Next.js**.

O comando `pnpm create next-app` serve para criar aplicações full-stack ou frontends focados em React. Como você quer construir uma API robusta e isolada usando Fastify, Zod e Better Auth, a fundação correta é um projeto **Node.js puro com TypeScript**. O Next.js (ou React Native) será usado em um repositório separado no futuro, quando formos fazer a interface.

Aqui está o passo a passo exato para inicializar o **Nummus API** com o `pnpm` e, em seguida, o prompt mestre para você colar no Claude Code e deixar ele montar a arquitetura.

### Passo 1: Inicialização Manual Básica

Abra o seu terminal e execute estes comandos um por vez para criar a pasta e iniciar o projeto vazio:

```bash
# 1. Cria a pasta do projeto e entra nela
mkdir nummus-api
cd nummus-api

# 2. Inicia um projeto Node.js vazio com pnpm
pnpm init

# 3. Instala as ferramentas básicas de TypeScript
pnpm add -D typescript @types/node tsx

# 4. Cria o arquivo de configuração do TypeScript (tsconfig.json)
npx tsc --init

```

---

### Passo 2: O Prompt Mestre para o Claude Code

Agora que você tem um diretório Node inicializado, abra o **Claude Code** dentro da pasta `nummus-api` e cole o prompt abaixo.

Note que eu já adaptei o arquivo `index` que você me mandou para usar as rotas do Nummus e inseri as instruções funcionais e de padronização do projeto.

**Copie e cole este bloco no Claude Code:**

**Objetivo:** Estruturar a base da "Nummus API" (um sistema de controle financeiro).
**Stack Tecnológica:** Node.js, Fastify, TypeScript, Zod, Prisma ORM, Better Auth e PostgreSQL.
**Paradigma Arquitetural Obrigatório:** Estritamente Funcional.

- É proibido o uso de `class`. Todos os _use cases_ e controladores devem ser funções puras ou assíncronas (ex: `export const createTransaction = async (...) = {}`).

**Tarefas a executar:**

1.  Instale as dependências necessárias para a stack (Fastify, cors, swagger, scalar, fastify-type-provider-zod, zod, prisma, dotenv, pino, better-auth, etc).
2.  Crie o `schema.prisma` com os modelos base para controle financeiro (além dos modelos do Better Auth):

- `Wallet`: id, name, balance, userId.
- `Category`: id, name, color, userId.
- `Transaction`: id, amount, type (INCOME, EXPENSE, TRANSFER), date, description, walletId, categoryId, userId.

3.  Crie a estrutura de pastas: `src/routes`, `src/usecases`, `src/schemas`, `src/lib`.
4.  Crie o arquivo principal de entrada (`src/server.ts` ou `src/index.ts`) usando **exatamente** o código abaixo:

```typescript
import "dotenv/config";
import fastifyCors from "@fastify/cors";
import fastifySwagger from "@fastify/swagger";
import fastifyApiReference from "@scalar/fastify-api-reference";
import Fastify from "fastify";
import {
  jsonSchemaTransform,
  jsonSchemaTransformObject,
  serializerCompiler,
  validatorCompiler,
  ZodTypeProvider,
} from "fastify-type-provider-zod";
import z from "zod";

import { auth } from "./lib/auth.js";
import { env } from "./lib/env.js";
import { transactionRoutes } from "./routes/transactions.js";
import { walletRoutes } from "./routes/wallets.js";
import { categoryRoutes } from "./routes/categories.js";
import { aiRoutes } from "./routes/ai.js";

const envToLogger = {
  development: { transport: { target: "pino-pretty", options: { translateTime: "HH:MM:ss Z", ignore: "pid,hostname" } } },
  production: true,
  test: false,
};

const app = Fastify({ logger: envToLogger[env.NODE_ENV] });

app.setValidatorCompiler(validatorCompiler);
app.setSerializerCompiler(serializerCompiler);

await app.register(fastifySwagger, {
  openapi: {
    info: { title: "nummus-api", description: "API para controle financeiro", version: "1.0.0" },
    servers: [{ url: env.API_BASE_URL, description: "API Base Url" }],
  },
  transform: jsonSchemaTransform,
  transformObject: jsonSchemaTransformObject,
});

app.get("/openapi.json", async () = {
  await app.ready();
  return app.swagger();
});

await app.register(fastifyApiReference, {
  routePrefix: "/documentation",
  configuration: {
    sources: [
      { title: "Nummus API", slug: "nummus-api", url: "/openapi.json" },
      { title: "Auth API", slug: "auth-api", url: "/api/auth/open-api/generate-schema" },
    ],
  },
});

await app.register(fastifyCors, { origin: [env.WEB_APP_BASE_URL], credentials: true });

// Routes
await app.register(transactionRoutes, { prefix: "/transactions" });
await app.register(walletRoutes, { prefix: "/wallets" });
await app.register(categoryRoutes, { prefix: "/categories" });
await app.register(aiRoutes, { prefix: "/ai" });

app.withTypeProvider<ZodTypeProvider().route({
  method: "GET",
  url: "/",
  schema: { description: "Health check", tags: ["Health"], response: { 200: z.object({ message: z.string() }) } },
  handler: async () = ({ message: "Nummus API is running 🚀" }),
});

app.route({
  method: ["GET", "POST", "OPTIONS"],
  url: "/api/auth/*",
  schema: { hide: true },
  async handler(request, reply) {
    if (request.method === "OPTIONS") return reply.status(204).send();
    try {
      const url = new URL(request.url, `http://${request.headers.host}`);
      const headers = new Headers();
      Object.entries(request.headers).forEach(([key, value]) = { if (value) headers.append(key, value.toString()); });
      const req = new Request(url.toString(), {
        method: request.method,
        headers,
        body: request.method !== "GET" && request.body ? JSON.stringify(request.body) : undefined,
      });
      const response = await auth.handler(req);
      reply.status(response.status);
      response.headers.forEach((value, key) = { reply.header(key, value); });
      reply.send(response.body);
    } catch (error) {
      app.log.error(error, "Authentication error");
      reply.status(500).send({ error: "Internal authentication error", code: "AUTH_FAILURE" });
    }
  },
});

try {
  await app.listen({ port: Number(env.PORT), host: "0.0.0.0" });
  app.log.info(`Server running at ${env.API_BASE_URL}`);
  app.log.info(`API Docs available at ${env.API_BASE_URL}/documentation`);
} catch (err) {
  app.log.error(err);
  process.exit(1);
}

```

5.  Crie arquivos fictícios simples em `src/routes` que apenas exportem as funções de rotas (para que o `index.ts` não quebre ao importar).
6.  **Regras de Padronização e Versionamento do Projeto:**

- Crie um arquivo de regras de desenvolvimento na raiz explicando que a documentação técnica deve ser em Português, mas as mensagens de commit em Inglês.
- Anote no README a seguinte instrução técnica de UI para os futuros clientes da API: "Todas as interfaces focadas (modais, pop-ups, janelas ativas) devem aplicar uma opacidade de 90%, garantindo que o contexto da aplicação (dashboard) continue visível ao fundo."
- Quaisquer commits automáticos feitos por você a partir de agora devem respeitar a regra do idioma Inglês.

```

```
